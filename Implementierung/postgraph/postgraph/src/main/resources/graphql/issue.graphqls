
type Issue{
    iid : ID!
    title : String
    state : String
    stateReason : String
    assignees : [Person]
    project : Project
}

extend type Query{
    issues : [Issue]
    issue(id: ID!): Issue
}