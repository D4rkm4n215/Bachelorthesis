type Person{
    pid : ID!
    firstname : String
    lastname : String
    email : String
    projects : [Project]
    project(id: ID!): Project
    issues : [Issue]
}

extend type Query{
    persons: [Person]
    person(id:ID!): Person
}